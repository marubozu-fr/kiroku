/// <reference types="vite/client" />

/**
 * App version injected at build time from the repository-root VERSION file
 * (see `define` in `vite.config.ts`).
 */
declare const __APP_VERSION__: string
